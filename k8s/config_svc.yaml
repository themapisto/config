apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: config_server
  name: config
  namespace: springboot
spec:
  ports:
  - port: 8088
    protocol: TCP
    targetPort: 8088
  selector:
    app: config_server
  type: ClusterIP
status:
  loadBalancer: {}
