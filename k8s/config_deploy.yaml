apiVersion: apps/v1
kind: Deployment
metadata:
  name: config
  namespace: springboot
  labels:
    app2: koo
spec:
  replicas: 1
  selector:
    matchLabels:
      app2: koo
  template:
    metadata:
      labels:
        app2: koo
    spec:
      containers:
      - name: config
        image: harbor.aikoo.net/tanzu/config:1.1
        ports:
        - containerPort: 8088